//only object type data for interface 
// Interface and type work same 
// type alias for all type data 


interface IHeroInterface {
    name: string,
    age: number
}

type HeroType1 = {
    name: string;
    age: number
}
// interface  z  number

type ABC = number;

const hero7: IHeroInterface = {
    name: "samim",
    age: 34
}
